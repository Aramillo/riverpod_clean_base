import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder - app requires SharedPreferences and dotenv initialization
    expect(true, isTrue);
  });
}
